Hey now we have made it according to properly compiling amoung our replit now listen I have , I forked https://github.com/vishkx/nextjs16_realtime_chat (see docs)  into my Replit project. This is a solid Next.js 16 real-time chat with self-destructing rooms and great Tailwind UI/UX, but it lacks user auth—users share room links externally, which sucks for privacy. Upgrade it to a full user system where people create unique shareable IDs (like a personal username/handle) in real life, visit the site, search/find profiles, follow, and start private rooms directly. No email/Google/Clerk—build custom auth from scratch for max privacy. Keep everything else intact: UI, rooms, real-time Socket.IO. Make it deploy-ready for Vercel. Here's the FULL PLAN—implement step-by-step, show code changes, test everything.**
### **1. Database Setup (MongoDB Atlas Free Tier)**
- Add Mongoose for Next.js API routes.
- Create these models:
  - **User**: `{ username: String (unique, 4-20 chars, alphanumeric+_, public shareable ID), hash: String (bcrypt password), followers: [username], following: [username], createdAt: Date }`
  - **Room**: `{ roomId: String (unique short code like 'abc123'), creatorUsername: String, participants: [username], messages: [{user: username, text: String, timestamp: Date}], expiresAt: Date (self-destruct), isPrivate: true }`
- Connect in `/lib/db.js` with env vars: `MONGODB_URI`.
- Seed no data—users create fresh.
### **2. Custom Auth System (No Libs, Pure Crypto/BCrypt)**
- Use `bcryptjs` for hashing, `crypto` for sessions.
- **/api/auth/register** (POST): 
  - Body: `{ username, password }`
  - Validate username unique (lowercase, no spaces).
  - Hash pw, save User. Return `{ success: true, username }`. Rate limit 5/min.
- **/api/auth/login** (POST): 
  - Body: `{ username, password }`
  - Compare hash, if good: generate session token (crypto.randomUUID() + sign with secret), set httpOnly cookie `authToken`, return `{ success: true, username }`.
- **/api/auth/logout** (POST): Clear cookie.
- **/api/auth/me** (GET): Verify cookie token, return user data or 401.
- Middleware `/middleware.js`: Protect pages/api/* and app pages with token verify func.
- Sessions: Store token-user map in Redis (Upstash free) or Mongo cache (expire 30 days).
### **3. Frontend Auth Pages (Keep Tailwind UI Style)**
- **/login**: Form username/pw, submit to /api/auth/login, redirect /dashboard.
- **/register**: Form username (show "share this ID IRL!"), pw (min 8 chars), submit /api/auth/register.
- **/dashboard** (protected): Show your username big, "Share this ID with friends: @yourusername". List following/followers.
- Navbar: Login/Register | Profile | Logout. Auto-redirect if logged in.
### **4. User Profiles & Discovery**
- **/[username]** (dynamic page, public): 
  - Fetch user by username.
  - Show: Avatar placeholder, "Follow to chat", bio input? No, keep minimal.
  - If logged in & not self: "Follow" button → POST /api/users/follow, add to followers.
- **Search**: /search?q=query → List users matching username (fuzzy, lowercase).
- Follow: Mutual? No, one-way: You follow → can create room with them.
### **5. Private Room Creation (Core Upgrade)**
- From dashboard or profile: "Create Room with @otherusername" button.
- POST /api/rooms/create: `{ participants: [yourUser, otherUser] }`
- Auto-generate roomId (short random 6 chars).
- Save Room with participants, redirect /room/[roomId].
- Only participants can join (check auth & username in list).
### **6. Chat Room Enhancements (Build on Existing)**
- **/room/[roomId]** (protected): 
  - Verify user in room.participants.
  - Real-time: Socket.IO join roomId, send/receive msgs.
  - Add self-destruct timer selector (1h, 24h, never).
  - UI: Existing chat + "Lock Room" (set pw? Optional PIN for extra layer).
- Socket events: 'join-room', 'send-message', 'leave-room'.
- Messages delete on expiresAt via cron job (Vercel cron) or Socket disconnect check.
### **7. Privacy & Security Must-Haves**
- E2E? Simulate: Client-side encrypt msgs with simple XOR key shared on room create (derive from usernames).
- No emails/phone—username ONLY shareable ID.
- Rate limits: 100 msgs/min/room.
- HTTPS only, sanitize inputs.
- Profiles public but chats locked to participants.
### **8. Polish & Deploy**
- Env vars: `JWT_SECRET`, `MONGODB_URI`, `REDIS_URL?`.
- Mobile-responsive (already Tailwind).
- Error pages: 404 user not found, 401 unauthorized.
- Test: Register 2 users, follow, create room, chat realtime.
- Deploy: Vercel—add vercel.json for rewrites.
**Replit, start by adding package.json deps: mongoose bcryptjs jsonwebtoken socket.io-client. Then implement DB connect. Show ALL code diffs/files created. Make it bulletproof for my friend's private couple chats—unique ID shared IRL, no links needed! Go step-by-step,
And bro we have to make it advanced ui /ux best practices new using existing library and bro one problem when the room is created it's working but it's not properly responsive (it's dragging out of screen ) and chat ui needed to improve also and 

Bro - this is my mongo cluster - mongodb+srv://asapadmin:<db_password>@aspchat.1t87bil.mongodb.net/?appName=Aspchat
And our app name aspzap.

DONT TAKE THIS PROJECT A PROTOTYPE OR SOMETHING.!! ITS A PRODUCTION LEVEL ( WE CAN SAY COUPLE SECRET CHAT APP) YOU HAVE SEEN THE PLAN ABOVE YOU CAN ALSO USE YOUR MIND TO CONFIGURE THE PLAN MORE BEST.